package com.uni.todoary.feature.setting.repository

import android.content.Context
import com.uni.todoary.feature.auth.data.dto.User

class Repository(private val context: Context) {
    fun getUser() : User{
        return getUser()
    }
}